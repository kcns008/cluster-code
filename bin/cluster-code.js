#!/usr/bin/env node

/**
 * Cluster Code CLI - Binary entry point
 *
 * This is the executable that gets installed globally when users run:
 * npm install -g @cluster-code/cluster-code
 */

require('../dist/cli.js');
